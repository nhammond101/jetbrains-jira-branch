# Suppress the Kotlin stdlib conflict warning from the IntelliJ Platform Gradle Plugin
kotlin.stdlib.default.dependency=false

# Default plugin version for local builds; CI releases can override with -PpluginVersion.
pluginVersion=1.2.0

# Suppress Gradle daemon warning (optional, for CI)
org.gradle.daemon=false

